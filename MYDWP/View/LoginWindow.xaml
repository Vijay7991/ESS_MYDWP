<UserControl x:Class="MYDWP.View.LoginWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="350" d:DesignWidth="350">
    <Grid Background="AntiqueWhite" >
        <Grid Width="300" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Login"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20" />

            <Grid Grid.Row="1" Margin="0,10" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Username" Grid.Column="0" VerticalAlignment="Center" FontSize="14" />
                <Border CornerRadius="10" Grid.Column="1" VerticalAlignment="Center" Margin="5" BorderThickness="1" BorderBrush="Black">
                    <TextBox BorderThickness="0" Background="Transparent" BorderBrush="Transparent" Padding="5"  Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
                </Border>
            </Grid>

            <Grid Grid.Row="2" Margin="0,10" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Password" Grid.Column="0" VerticalAlignment="Center" FontSize="14" />
                <Border CornerRadius="10" Grid.Column="1" VerticalAlignment="Center" Margin="5" BorderThickness="1" BorderBrush="Black">
                    <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Padding="5" Background="Transparent" BorderBrush="Transparent"/>
                </Border>
               
                
                
            </Grid>

            <Border Grid.Row="3" VerticalAlignment="Center" CornerRadius="10" BorderBrush="Green" BorderThickness="1" Background="LightBlue" >
            <Button  Padding="8"
                    Content="Login" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                    FontWeight="Bold"
                    Command="{Binding LoginCommand}" />
            </Border>

                <Button  Padding="8" Grid.Row="4" HorizontalAlignment="Left" Foreground="Blue" 
                    Content="Registor" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                    FontWeight="Bold"
                    Command="{Binding RegisterCommand}" />

        </Grid>
    </Grid>
</UserControl>
